# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
requests>=2.31.0
matplotlib>=3.7.0
Pillow>=10.0.0

# PyTorch (pinned to CUDA 11.7 build for the lab GPU box; driver caps at CUDA 12.8 but the env was set up against cu117)
# Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu117
# For CPU-only or Mac MPS, install torch/torchvision/torchaudio separately and skip the +cu117 pins below.
torch==2.0.0+cu117
torchvision==0.15.1+cu117
torchaudio==2.0.1+cu117

# Logging and visualization
tensorboard>=2.13.0
scipy
opencv-python>=4.8.0
timm>=1.0.0
scikit-learn>=1.3.0
